{% extends 'base.html.twig' %}

{% block title %}SLList{% endblock %}

{% block body %}
    <h1>SLList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ s_l_list.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_s_l_list_index') }}">back to list</a>

    <a href="{{ path('app_s_l_list_edit', {'id': s_l_list.id}) }}">edit</a>

    {{ include('sl_list/_delete_form.html.twig') }}
{% endblock %}
