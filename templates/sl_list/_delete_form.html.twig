<form method="post" action="{{ path('app_s_l_list_delete', {'id': s_l_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ s_l_list.id) }}">
    <button class="btn">Delete</button>
</form>
