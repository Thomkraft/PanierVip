<form method="post" action="{{ path('app_spent_in_category_delete', {'id': spent_in_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ spent_in_category.id) }}">
    <button class="btn">Delete</button>
</form>
