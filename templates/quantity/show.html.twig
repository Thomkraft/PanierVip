{% extends 'base.html.twig' %}

{% block title %}Quantity{% endblock %}

{% block body %}
    <h1>Quantity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quantity.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ quantity.quantity }}</td>
            </tr>
            <tr>
                <th>Bought</th>
                <td>{{ quantity.bought ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quantity_index') }}">back to list</a>

    <a href="{{ path('app_quantity_edit', {'id': quantity.id}) }}">edit</a>

    {{ include('quantity/_delete_form.html.twig') }}
{% endblock %}
